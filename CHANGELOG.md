# سجل التغييرات — نموذج تقييم مقابلة شخصية وفنية (ICTD)

## v1.5 — 2026-09-22
- **تحسين شكلي:** قوائم التقييم المنسدلة (عمود "التقييم" في جدول التقييم الفني، وقائمة "المستوى الفني العام") أصبحت تُلوَّن تلقائيًا على شكل حبة (Pill) بلون يعكس القيمة المختارة، بدل القائمة البيضاء العادية.
- نظام الألوان المعتمد:
  - **ممتاز**: أخضر داكن (تعبئة كاملة، نص أبيض)
  - **جيد جدا / جيد**: أخضر فاتح، نص أخضر داكن
  - **مقبول**: أزرق (تعبئة كاملة، نص أبيض)
  - **مناسب**: أزرق فاتح، نص أزرق
  - **ضعيف / فجوة**: وردي فاتح، نص أحمر
  - **غير مقيّم**: رمادي فاتح، نص رمادي داكن
- الألوان تتحدّث فوريًا عند تغيير القيمة، وتُطبَّق تلقائيًا أيضًا عند تحميل سجل محفوظ سابقًا (ميزة v1.2) أو عند بدء مقابلة جديدة.
- عند الطباعة/حفظ PDF، يحافظ النص المطبوع لهذه القوائم على نفس شكل الحبة الملوّنة (وليس نصًا عاديًا فقط)، مع تفعيل خاصية طباعة الألوان (`print-color-adjust: exact`) — لإظهار الألوان يجب التأكد من تفعيل خيار "الرسومات الخلفية / Background graphics" في نافذة طباعة المتصفح.
- لا تغيير على `Code.gs` أو على الحفظ في Google Sheets.

## v1.4 — 2026-09-22
- **إصلاح:** كانت نسخة v1.3 تُظهر نص كل حقل **مكررًا مرتين** عند الطباعة/حفظ PDF.
- السبب: زر الطباعة استدعى دالة بناء النص الكامل (`buildPrintValues`) يدويًا، ثم أطلق `window.print()` الذي يُشغّل حدث `beforeprint` في المتصفح تلقائيًا — وهذا الحدث كان مربوطًا بنفس الدالة، فتُستدعى مرتين وتُدرج نسختين من كل نص.
- الحل: أصبحت `buildPrintValues` تُفرغ أي نسخ سابقة من النص المطبوع تلقائيًا (idempotent) في بداية تنفيذها، فلا يظهر أي تكرار بغض النظر عن عدد مرات استدعائها.
- لا تغيير على `Code.gs` أو على الحفظ في Google Sheets.

## v1.3 — 2026-09-22
- **إصلاح:** عند الطباعة/حفظ PDF، كان نص الحقول (حقول جدول التقييم، القوائم المنسدلة كالتقييم/المستوى العام، الملاحظات الطويلة) يظهر مقصوصًا لأن عناصر `<input>`/`<textarea>`/`<select>` لا تتمدد تلقائيًا لإظهار كامل محتواها عند الطباعة.
- الحل: قبل فتح نافذة الطباعة مباشرة، يستبدل الفورم كل حقل نصي أو قائمة منسدلة بنص كامل غير مقصوص بنفس مكانه، ثم يعيد الحقول القابلة للتعديل تلقائيًا بعد إغلاق نافذة الطباعة أو إلغائها.
- تمت إضافة `page-break-inside:avoid` لصفوف جدول التقييم حتى لا ينقسم صف واحد بين صفحتين عند الطباعة.
- لا تغيير وظيفي على `Code.gs` أو على الحفظ في Google Sheets — الإصلاح يخص عرض/طباعة الصفحة فقط.

## v1.2 — 2026-09-22
- ميزة جديدة: شريط **"📂 تحميل مقابلة محفوظة"** أعلى الفورم — قائمة منسدلة تُحمَّل تلقائيًا من Google Sheets وتعرض كل سجل بصيغة (التاريخ — اسم المرشح — الوظيفة — الرقم المرجعي).
- زر **"تحميل للمراجعة والتعديل"**: يجلب تفاصيل السجل المحدد كاملة (بيانات المرشح، جدول التقييم الفني بكل صفوفه، التقييم العام، نص وتوصيات، والتوقيعات) ويملأ بها الفورم مباشرة للمراجعة والتعديل.
- زر **"↻ تحديث القائمة"** لإعادة جلب أحدث قائمة سجلات من الشيت دون إعادة تحميل الصفحة.
- إعادة الحفظ بعد التعديل تُحدّث نفس السجل (بنفس الرقم المرجعي) بدل إنشاء سجل مكرر — بالاعتماد على منطق التحديث الموجود مسبقًا في `Code.gs`.
- `Code.gs`: تم توسيع `doGet` ليدعم `?action=list` (قائمة مختصرة بكل السجلات) و `?action=get&formId=...` (تفاصيل سجل واحد كاملة، بما في ذلك تفكيك حقل JSON الخاص بجدول التقييم الفني ونقاط التوصية).
- **يتطلب إعادة نشر `Code.gs` المحدّث على نفس الـ Web App الحالي** (Deploy → Manage deployments → Edit → New version) — رابط `/exec` يبقى كما هو، التفاصيل في `README.md`.

## v1.1 — 2026-09-08
- توسيع قائمة تقييم كل مجال (Dropdown) لتشمل: ممتاز - جيد جدا - جيد - مقبول - مناسب - ضعيف - غير مقيّم (بدل القائمة المختصرة السابقة).
- نفس القائمة الموسّعة طُبّقت أيضًا على قائمة "المستوى الفني العام".

## v1.0 — 2026-09-08
- الإصدار الأول: تحويل نموذج تقييم المقابلة إلى **فورم عام** قابل لإعادة الاستخدام لأي مقابلة جديدة (وليس مخصصًا لمرشح واحد).
- حقول أساسية: اسم المرشح، تاريخ المقابلة، الوظيفة، اسم القائم بالمقابلة.
- جدول تقييم فني ديناميكي: إضافة/حذف مجالات تقييم بحرية، مع تقييم لكل مجال (جيد / مقبول / فجوة / غير مقيّم).
- قسم التقييم العام (مستوى عام + ملخص نصي).
- قسم التوصية بنص تمهيدي + قائمة نقاط ديناميكية (إضافة/حذف).
- خانتا توقيع/اعتماد.
- **رقم مرجعي فريد تلقائي** لكل نموذج بصيغة `INTV-YYYYMMDD-HHMMXXXX` يظهر أعلى الصفحة ويُرسل مع البيانات.
- زر **"حفظ في Google Sheets"** يرسل بيانات النموذج عبر Apps Script Web App (`Code.gs` المرفق) إلى شيت مخصص؛ إعادة الحفظ لنفس الرقم المرجعي تُحدّث نفس الصف بدل تكراره.
- زر **"طباعة / حفظ PDF"** لإخراج نسخة نظيفة (تختفي الأزرار وعناصر التحكم تلقائيًا عند الطباعة).
- زر **"مقابلة جديدة"** لتفريغ النموذج بالكامل وتوليد رقم مرجعي جديد دون إعادة تحميل الصفحة.
- الهوية البصرية الكاملة لـ ICTD / SRU (الألوان، الشعار، الاتجاه RTL، خط Cairo).
- تحديث خانة الاعتماد الثانية لتصبح **"اعتماد الرئيس التنفيذي للإدارة التنفيذية للاتصالات وتقنية المعلومات"** مع تعبئة الاسم مسبقًا (د. ناصر أبو نار).
- تم إنشاء Google Sheet الفعلي "سجل تقييم المقابلات - ICTD" في Drive المستخدم وربط `Code.gs` به.
- تم تثبيت رابط Web App النهائي (`SHEET_URL`) داخل الفورم — الحفظ في Google Sheets يعمل مباشرة دون أي إعداد إضافي.
