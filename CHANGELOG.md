# سجل التغييرات — نموذج تقييم مقابلة شخصية وفنية (ICTD)

## v1.0 — 2026-09-08
- الإصدار الأول: تحويل نموذج تقييم المقابلة إلى **فورم عام** قابل لإعادة الاستخدام لأي مقابلة جديدة (وليس مخصصًا لمرشح واحد).
- حقول أساسية: اسم المرشح، تاريخ المقابلة، الوظيفة، اسم القائم بالمقابلة.
- جدول تقييم فني ديناميكي: إضافة/حذف مجالات تقييم بحرية، مع تقييم لكل مجال (جيد / مقبول / فجوة / غير مقيّم).
- قسم التقييم العام (مستوى عام + ملخص نصي).
- قسم التوصية بنص تمهيدي + قائمة نقاط ديناميكية (إضافة/حذف).
- خانتا توقيع/اعتماد.
- **رقم مرجعي فريد تلقائي** لكل نموذج بصيغة `INTV-YYYYMMDD-HHMMXXXX` يظهر أعلى الصفحة ويُرسل مع البيانات.
- زر **"حفظ في Google Sheets"** يرسل بيانات النموذج عبر Apps Script Web App (`Code.gs` المرفق) إلى شيت مخصص؛ إعادة الحفظ لنفس الرقم المرجعي تُحدّث نفس الصف بدل تكراره.
- زر **"طباعة / حفظ PDF"** لإخراج نسخة نظيفة (تختفي الأزرار وعناصر التحكم تلقائيًا عند الطباعة).
- زر **"مقابلة جديدة"** لتفريغ النموذج بالكامل وتوليد رقم مرجعي جديد دون إعادة تحميل الصفحة.
- الهوية البصرية الكاملة لـ ICTD / SRU (الألوان، الشعار، الاتجاه RTL، خط Cairo).
- تحديث خانة الاعتماد الثانية لتصبح **"اعتماد الرئيس التنفيذي للإدارة التنفيذية للاتصالات وتقنية المعلومات"** مع تعبئة الاسم مسبقًا (د. ناصر أبو نار).
- تم إنشاء Google Sheet الفعلي "سجل تقييم المقابلات - ICTD" في Drive المستخدم وربط `Code.gs` به.
- تم تثبيت رابط Web App النهائي (`SHEET_URL`) داخل الفورم — الحفظ في Google Sheets يعمل مباشرة دون أي إعداد إضافي.
